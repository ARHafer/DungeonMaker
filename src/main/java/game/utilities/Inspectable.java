package game.utilities;

/*
 * Every class implementing this interface must have an inspectString method.
 */

public interface Inspectable {
    String inspectString(); // The string displayed when an object is inspected by the player.
}
